<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://java.sun.com/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://java.sun.com/xml/ns/persistence http://java.sun.com/xml/ns/persistence/persistence_2_0.xsd"
             version="2.0">

    <persistence-unit name="MyAppPersistenceUnit" transaction-type="RESOURCE_LOCAL">
        
        <!-- Managed Entity Classes (optional if entities are auto-discovered) -->
        <!-- Add more as needed -->

        <properties>
            <!-- Database connection details -->
            <property name="javax.persistence.jdbc.driver" value="org.h2.Driver"/>
            <property name="javax.persistence.jdbc.url" value="jdbc:h2:file:./data/myDatabase;DB_CLOSE_ON_EXIT=FALSE;AUTO_SERVER=TRUE"/>
            <property name="javax.persistence.jdbc.user" value="sa"/>
            <property name="javax.persistence.jdbc.password" value="sa"/>
            <property name="javax.persistence.schema-generation.database.action" value="update"/>

            
            <!-- Hibernate specific configurations -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.H2Dialect"/>
            <property name="hibernate.show_sql" value="false"/>
            <property name="hibernate.format_sql" value="true"/>
            <property name="hibernate.use_sql_comments" value="false"/>
            <property name="hibernate.hbm2ddl.auto" value="update"/> <!-- Automatically updates the database schema when changes are detected -->

            <!-- Add other properties as needed -->

        </properties>

    </persistence-unit>

</persistence>
